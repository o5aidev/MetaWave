import XCTest

final class MetaCognitionUITests: XCTestCase {
    func testExample() throws {
        throw XCTSkip("Skip UI tests for now")
    }

    @MainActor
    func testLaunchPerformance() throws {
        throw XCTSkip("Skip launch performance for now")
    }
}
