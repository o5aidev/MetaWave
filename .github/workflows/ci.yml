
name: iOS CI

on:
  push:
    branches: [ main, develop, feature/**, fix/**, ci/** ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: macos-latest

    concurrency:
      group: ios-ci-${{ github.ref }}
      cancel-in-progress: true

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: "16.4.0"

      - name: Install Bundler dependencies
        if: ${{ hashFiles('Gemfile') != '' }}
        run: bundle install --path vendor/bundle

      - name: Install CocoaPods (if needed)
        if: ${{ hashFiles('Podfile') != '' }}
        run: pod install

      - name: List available simulators
        run: xcrun simctl list devices available

      - name: Show available destinations
        working-directory: MetaWave
        run: xcodebuild -project MetaWave.xcodeproj -scheme MetaWave -showdestinations

      - name: Build
        working-directory: MetaWave
        run: |
          set -o pipefail
          set -x
          # Get the first available iOS Simulator destination from xcodebuild
          echo "Fetching available destinations..."
          DEST_OUTPUT=$(xcodebuild -project MetaWave.xcodeproj -scheme MetaWave -showdestinations 2>&1)
          echo "Destinations output:"
          echo "$DEST_OUTPUT"
          # Extract first iPhone simulator ID (excluding placeholders, prefer iPhone over iPad)
          DEST=$(echo "$DEST_OUTPUT" | grep "platform:iOS Simulator" | grep -i "iPhone" | grep -v "placeholder" | head -1 | sed -E 's/.*id:([A-F0-9-]+).*/\1/' | tr -d ' ')
          if [ -z "$DEST" ]; then
            # Fallback to any iOS Simulator (excluding placeholders)
            DEST=$(echo "$DEST_OUTPUT" | grep "platform:iOS Simulator" | grep -v "placeholder" | head -1 | sed -E 's/.*id:([A-F0-9-]+).*/\1/' | tr -d ' ')
          fi
          echo "Extracted destination ID: '$DEST'"
          if [ -z "$DEST" ]; then
            echo "No simulator destination found, trying with SDK only"
            xcodebuild -project MetaWave.xcodeproj \
              -scheme "MetaWave" \
              -configuration Debug \
              -sdk iphonesimulator \
              build 2>&1 | tee build.log || (cat build.log && exit 1)
          else
            echo "Using destination: $DEST"
            xcodebuild -project MetaWave.xcodeproj \
              -scheme "MetaWave" \
              -configuration Debug \
              -destination "platform=iOS Simulator,id=$DEST" \
              build 2>&1 | tee build.log || (cat build.log && exit 1)
          fi

      - name: Test
        working-directory: MetaWave
        run: |
          set -o pipefail
          set -x
          # Get the first available iOS Simulator destination from xcodebuild
          echo "Fetching available destinations..."
          DEST_OUTPUT=$(xcodebuild -project MetaWave.xcodeproj -scheme MetaWave -showdestinations 2>&1)
          # Extract first iPhone simulator ID (excluding placeholders, prefer iPhone over iPad)
          DEST=$(echo "$DEST_OUTPUT" | grep "platform:iOS Simulator" | grep -i "iPhone" | grep -v "placeholder" | head -1 | sed -E 's/.*id:([A-F0-9-]+).*/\1/' | tr -d ' ')
          if [ -z "$DEST" ]; then
            # Fallback to any iOS Simulator (excluding placeholders)
            DEST=$(echo "$DEST_OUTPUT" | grep "platform:iOS Simulator" | grep -v "placeholder" | head -1 | sed -E 's/.*id:([A-F0-9-]+).*/\1/' | tr -d ' ')
          fi
          echo "Extracted destination ID: '$DEST'"
          if [ -z "$DEST" ]; then
            echo "No simulator destination found, trying with SDK only"
            xcodebuild -project MetaWave.xcodeproj \
              -scheme "MetaWave" \
              -configuration Debug \
              -sdk iphonesimulator \
              test 2>&1 | tee test.log || (cat test.log && exit 1)
          else
            echo "Using destination: $DEST"
            xcodebuild -project MetaWave.xcodeproj \
              -scheme "MetaWave" \
              -configuration Debug \
              -destination "platform=iOS Simulator,id=$DEST" \
              test 2>&1 | tee test.log || (cat test.log && exit 1)
          fi

      - name: Archive DerivedData logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: xcode-logs
          path: ~/Library/Logs/DiagnosticReports
          if-no-files-found: ignore
